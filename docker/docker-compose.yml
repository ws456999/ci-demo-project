version: '3'

services:
  ts:
    build:
      context: ./docker
      dockerfile: Dockerfile
    container_name: "react-ts"
    build: .
    ports:
     - "4200:4200"
    volumes:
     - "/39/ci-demo-project/docker/nginx/conf.d:/etc/nginx/conf.d:rw"